/* Generated by Spago v1.0.4 */
(() => {
  // output/Control.Semigroupoid/index.js
  var semigroupoidFn = {
    compose: function(f) {
      return function(g) {
        return function(x) {
          return f(g(x));
        };
      };
    }
  };

  // output/Control.Category/index.js
  var identity = function(dict) {
    return dict.identity;
  };
  var categoryFn = {
    identity: function(x) {
      return x;
    },
    Semigroupoid0: function() {
      return semigroupoidFn;
    }
  };

  // output/Data.Function/index.js
  var $$const = function(a2) {
    return function(v) {
      return a2;
    };
  };

  // output/Data.Functor/foreign.js
  var arrayMap = function(f) {
    return function(arr) {
      var l = arr.length;
      var result = new Array(l);
      for (var i = 0; i < l; i++) {
        result[i] = f(arr[i]);
      }
      return result;
    };
  };

  // output/Data.Unit/foreign.js
  var unit = void 0;

  // output/Type.Proxy/index.js
  var $$Proxy = /* @__PURE__ */ function() {
    function $$Proxy2() {
    }
    ;
    $$Proxy2.value = new $$Proxy2();
    return $$Proxy2;
  }();

  // output/Data.Functor/index.js
  var map = function(dict) {
    return dict.map;
  };
  var functorArray = {
    map: arrayMap
  };

  // output/Control.Apply/index.js
  var identity2 = /* @__PURE__ */ identity(categoryFn);
  var apply = function(dict) {
    return dict.apply;
  };
  var applySecond = function(dictApply) {
    var apply1 = apply(dictApply);
    var map5 = map(dictApply.Functor0());
    return function(a2) {
      return function(b) {
        return apply1(map5($$const(identity2))(a2))(b);
      };
    };
  };

  // output/Control.Applicative/index.js
  var pure = function(dict) {
    return dict.pure;
  };
  var when = function(dictApplicative) {
    var pure1 = pure(dictApplicative);
    return function(v) {
      return function(v1) {
        if (v) {
          return v1;
        }
        ;
        if (!v) {
          return pure1(unit);
        }
        ;
        throw new Error("Failed pattern match at Control.Applicative (line 63, column 1 - line 63, column 63): " + [v.constructor.name, v1.constructor.name]);
      };
    };
  };
  var liftA1 = function(dictApplicative) {
    var apply2 = apply(dictApplicative.Apply0());
    var pure1 = pure(dictApplicative);
    return function(f) {
      return function(a2) {
        return apply2(pure1(f))(a2);
      };
    };
  };

  // output/Data.Void/index.js
  var absurd = function(a2) {
    var spin = function($copy_v) {
      var $tco_result;
      function $tco_loop(v) {
        $copy_v = v;
        return;
      }
      ;
      while (true) {
        $tco_result = $tco_loop($copy_v);
      }
      ;
      return $tco_result;
    };
    return spin(a2);
  };

  // output/Effect/foreign.js
  var pureE = function(a2) {
    return function() {
      return a2;
    };
  };
  var bindE = function(a2) {
    return function(f) {
      return function() {
        return f(a2())();
      };
    };
  };

  // output/Control.Bind/index.js
  var bind = function(dict) {
    return dict.bind;
  };

  // output/Control.Monad/index.js
  var ap = function(dictMonad) {
    var bind2 = bind(dictMonad.Bind1());
    var pure4 = pure(dictMonad.Applicative0());
    return function(f) {
      return function(a2) {
        return bind2(f)(function(f$prime) {
          return bind2(a2)(function(a$prime) {
            return pure4(f$prime(a$prime));
          });
        });
      };
    };
  };

  // output/Data.Symbol/index.js
  var reflectSymbol = function(dict) {
    return dict.reflectSymbol;
  };

  // output/Data.Semigroup/foreign.js
  var concatArray = function(xs) {
    return function(ys) {
      if (xs.length === 0) return ys;
      if (ys.length === 0) return xs;
      return xs.concat(ys);
    };
  };

  // output/Data.Semigroup/index.js
  var semigroupArray = {
    append: concatArray
  };
  var append = function(dict) {
    return dict.append;
  };

  // output/Data.Monoid/index.js
  var mempty = function(dict) {
    return dict.mempty;
  };

  // output/Effect/index.js
  var $runtime_lazy = function(name, moduleName, init3) {
    var state = 0;
    var val;
    return function(lineNumber) {
      if (state === 2) return val;
      if (state === 1) throw new ReferenceError(name + " was needed before it finished initializing (module " + moduleName + ", line " + lineNumber + ")", moduleName, lineNumber);
      state = 1;
      val = init3();
      state = 2;
      return val;
    };
  };
  var monadEffect = {
    Applicative0: function() {
      return applicativeEffect;
    },
    Bind1: function() {
      return bindEffect;
    }
  };
  var bindEffect = {
    bind: bindE,
    Apply0: function() {
      return $lazy_applyEffect(0);
    }
  };
  var applicativeEffect = {
    pure: pureE,
    Apply0: function() {
      return $lazy_applyEffect(0);
    }
  };
  var $lazy_functorEffect = /* @__PURE__ */ $runtime_lazy("functorEffect", "Effect", function() {
    return {
      map: liftA1(applicativeEffect)
    };
  });
  var $lazy_applyEffect = /* @__PURE__ */ $runtime_lazy("applyEffect", "Effect", function() {
    return {
      apply: ap(monadEffect),
      Functor0: function() {
        return $lazy_functorEffect(0);
      }
    };
  });
  var applyEffect = /* @__PURE__ */ $lazy_applyEffect(23);

  // output/App.Runtime/index.js
  var pure2 = /* @__PURE__ */ pure(applicativeEffect);
  var onSubscription = function(v) {
    return function(v1) {
      return pure2(pure2(unit));
    };
  };
  var onCommand = absurd;

  // output/Data.Bounded/foreign.js
  var topChar = String.fromCharCode(65535);
  var bottomChar = String.fromCharCode(0);
  var topNumber = Number.POSITIVE_INFINITY;
  var bottomNumber = Number.NEGATIVE_INFINITY;

  // output/Data.Maybe/index.js
  var Nothing = /* @__PURE__ */ function() {
    function Nothing2() {
    }
    ;
    Nothing2.value = new Nothing2();
    return Nothing2;
  }();
  var Just = /* @__PURE__ */ function() {
    function Just2(value0) {
      this.value0 = value0;
    }
    ;
    Just2.create = function(value0) {
      return new Just2(value0);
    };
    return Just2;
  }();

  // output/Data.Array/foreign.js
  var replicateFill = function(count, value) {
    if (count < 1) {
      return [];
    }
    var result = new Array(count);
    return result.fill(value);
  };
  var replicatePolyfill = function(count, value) {
    var result = [];
    var n = 0;
    for (var i = 0; i < count; i++) {
      result[n++] = value;
    }
    return result;
  };
  var replicateImpl = typeof Array.prototype.fill === "function" ? replicateFill : replicatePolyfill;
  var unconsImpl = function(empty2, next, xs) {
    return xs.length === 0 ? empty2({}) : next(xs[0])(xs.slice(1));
  };
  var filterImpl = function(f, xs) {
    return xs.filter(f);
  };

  // output/Effect.Ref/foreign.js
  var _new = function(val) {
    return function() {
      return { value: val };
    };
  };
  var read = function(ref) {
    return function() {
      return ref.value;
    };
  };
  var write = function(val) {
    return function(ref) {
      return function() {
        ref.value = val;
      };
    };
  };

  // output/Effect.Ref/index.js
  var $$new = _new;

  // output/Data.HeytingAlgebra/foreign.js
  var boolConj = function(b1) {
    return function(b2) {
      return b1 && b2;
    };
  };
  var boolDisj = function(b1) {
    return function(b2) {
      return b1 || b2;
    };
  };
  var boolNot = function(b) {
    return !b;
  };

  // output/Data.HeytingAlgebra/index.js
  var tt = function(dict) {
    return dict.tt;
  };
  var not = function(dict) {
    return dict.not;
  };
  var disj = function(dict) {
    return dict.disj;
  };
  var heytingAlgebraBoolean = {
    ff: false,
    tt: true,
    implies: function(a2) {
      return function(b) {
        return disj(heytingAlgebraBoolean)(not(heytingAlgebraBoolean)(a2))(b);
      };
    },
    conj: boolConj,
    disj: boolDisj,
    not: boolNot
  };
  var conj = function(dict) {
    return dict.conj;
  };

  // output/Data.Foldable/foreign.js
  var foldrArray = function(f) {
    return function(init3) {
      return function(xs) {
        var acc = init3;
        var len = xs.length;
        for (var i = len - 1; i >= 0; i--) {
          acc = f(xs[i])(acc);
        }
        return acc;
      };
    };
  };
  var foldlArray = function(f) {
    return function(init3) {
      return function(xs) {
        var acc = init3;
        var len = xs.length;
        for (var i = 0; i < len; i++) {
          acc = f(acc)(xs[i]);
        }
        return acc;
      };
    };
  };

  // output/Data.Tuple/index.js
  var Tuple = /* @__PURE__ */ function() {
    function Tuple2(value0, value1) {
      this.value0 = value0;
      this.value1 = value1;
    }
    ;
    Tuple2.create = function(value0) {
      return function(value1) {
        return new Tuple2(value0, value1);
      };
    };
    return Tuple2;
  }();

  // output/Data.Monoid.Conj/index.js
  var Conj = function(x) {
    return x;
  };
  var semigroupConj = function(dictHeytingAlgebra) {
    var conj2 = conj(dictHeytingAlgebra);
    return {
      append: function(v) {
        return function(v1) {
          return conj2(v)(v1);
        };
      }
    };
  };
  var monoidConj = function(dictHeytingAlgebra) {
    var semigroupConj1 = semigroupConj(dictHeytingAlgebra);
    return {
      mempty: tt(dictHeytingAlgebra),
      Semigroup0: function() {
        return semigroupConj1;
      }
    };
  };

  // output/Unsafe.Coerce/foreign.js
  var unsafeCoerce2 = function(x) {
    return x;
  };

  // output/Safe.Coerce/index.js
  var coerce = function() {
    return unsafeCoerce2;
  };

  // output/Data.Newtype/index.js
  var coerce2 = /* @__PURE__ */ coerce();
  var alaF = function() {
    return function() {
      return function() {
        return function() {
          return function(v) {
            return coerce2;
          };
        };
      };
    };
  };

  // output/Data.Foldable/index.js
  var alaF2 = /* @__PURE__ */ alaF()()()();
  var foldr = function(dict) {
    return dict.foldr;
  };
  var traverse_ = function(dictApplicative) {
    var applySecond3 = applySecond(dictApplicative.Apply0());
    var pure4 = pure(dictApplicative);
    return function(dictFoldable) {
      var foldr2 = foldr(dictFoldable);
      return function(f) {
        return foldr2(function($454) {
          return applySecond3(f($454));
        })(pure4(unit));
      };
    };
  };
  var foldl = function(dict) {
    return dict.foldl;
  };
  var foldMapDefaultR = function(dictFoldable) {
    var foldr2 = foldr(dictFoldable);
    return function(dictMonoid) {
      var append3 = append(dictMonoid.Semigroup0());
      var mempty2 = mempty(dictMonoid);
      return function(f) {
        return foldr2(function(x) {
          return function(acc) {
            return append3(f(x))(acc);
          };
        })(mempty2);
      };
    };
  };
  var foldableArray = {
    foldr: foldrArray,
    foldl: foldlArray,
    foldMap: function(dictMonoid) {
      return foldMapDefaultR(foldableArray)(dictMonoid);
    }
  };
  var foldMap = function(dict) {
    return dict.foldMap;
  };
  var all = function(dictFoldable) {
    var foldMap2 = foldMap(dictFoldable);
    return function(dictHeytingAlgebra) {
      return alaF2(Conj)(foldMap2(monoidConj(dictHeytingAlgebra)));
    };
  };

  // output/Data.Function.Uncurried/foreign.js
  var runFn2 = function(fn) {
    return function(a2) {
      return function(b) {
        return fn(a2, b);
      };
    };
  };
  var runFn3 = function(fn) {
    return function(a2) {
      return function(b) {
        return function(c) {
          return fn(a2, b, c);
        };
      };
    };
  };

  // output/Data.Traversable/foreign.js
  var traverseArrayImpl = /* @__PURE__ */ function() {
    function array1(a2) {
      return [a2];
    }
    function array2(a2) {
      return function(b) {
        return [a2, b];
      };
    }
    function array3(a2) {
      return function(b) {
        return function(c) {
          return [a2, b, c];
        };
      };
    }
    function concat2(xs) {
      return function(ys) {
        return xs.concat(ys);
      };
    }
    return function(apply2) {
      return function(map5) {
        return function(pure4) {
          return function(f) {
            return function(array) {
              function go(bot, top2) {
                switch (top2 - bot) {
                  case 0:
                    return pure4([]);
                  case 1:
                    return map5(array1)(f(array[bot]));
                  case 2:
                    return apply2(map5(array2)(f(array[bot])))(f(array[bot + 1]));
                  case 3:
                    return apply2(apply2(map5(array3)(f(array[bot])))(f(array[bot + 1])))(f(array[bot + 2]));
                  default:
                    var pivot = bot + Math.floor((top2 - bot) / 4) * 2;
                    return apply2(map5(concat2)(go(bot, pivot)))(go(pivot, top2));
                }
              }
              return go(0, array.length);
            };
          };
        };
      };
    };
  }();

  // output/Data.Traversable/index.js
  var identity3 = /* @__PURE__ */ identity(categoryFn);
  var traverse = function(dict) {
    return dict.traverse;
  };
  var sequenceDefault = function(dictTraversable) {
    var traverse22 = traverse(dictTraversable);
    return function(dictApplicative) {
      return traverse22(dictApplicative)(identity3);
    };
  };
  var traversableArray = {
    traverse: function(dictApplicative) {
      var Apply0 = dictApplicative.Apply0();
      return traverseArrayImpl(apply(Apply0))(map(Apply0.Functor0()))(pure(dictApplicative));
    },
    sequence: function(dictApplicative) {
      return sequenceDefault(traversableArray)(dictApplicative);
    },
    Functor0: function() {
      return functorArray;
    },
    Foldable1: function() {
      return foldableArray;
    }
  };

  // output/Data.Array/index.js
  var uncons = /* @__PURE__ */ function() {
    return runFn3(unconsImpl)($$const(Nothing.value))(function(x) {
      return function(xs) {
        return new Just({
          head: x,
          tail: xs
        });
      };
    });
  }();
  var filter = /* @__PURE__ */ runFn2(filterImpl);

  // output/Data.String.CodeUnits/foreign.js
  var _indexOf = function(just) {
    return function(nothing) {
      return function(x) {
        return function(s) {
          var i = s.indexOf(x);
          return i === -1 ? nothing : just(i);
        };
      };
    };
  };
  var take = function(n) {
    return function(s) {
      return s.substr(0, n);
    };
  };
  var drop = function(n) {
    return function(s) {
      return s.substring(n);
    };
  };

  // output/Data.String.CodeUnits/index.js
  var indexOf = /* @__PURE__ */ function() {
    return _indexOf(Just.create)(Nothing.value);
  }();

  // output/Data.String.Common/foreign.js
  var split = function(sep) {
    return function(s) {
      return s.split(sep);
    };
  };

  // output/PsSpa.Option/index.js
  var None = /* @__PURE__ */ function() {
    function None3() {
    }
    ;
    None3.value = new None3();
    return None3;
  }();
  var Some = /* @__PURE__ */ function() {
    function Some2(value0) {
      this.value0 = value0;
    }
    ;
    Some2.create = function(value0) {
      return new Some2(value0);
    };
    return Some2;
  }();

  // output/PsSpa.UrlCodec/foreign.js
  var decodeURIComponent = function(value) {
    try {
      return globalThis.decodeURIComponent(String(value));
    } catch (_error) {
      return String(value);
    }
  };

  // output/Generated.Route/index.js
  var foldl2 = /* @__PURE__ */ foldl(foldableArray);
  var map2 = /* @__PURE__ */ map(functorArray);
  var Editor = /* @__PURE__ */ function() {
    function Editor2() {
    }
    ;
    Editor2.value = new Editor2();
    return Editor2;
  }();
  var Index = /* @__PURE__ */ function() {
    function Index2() {
    }
    ;
    Index2.value = new Index2();
    return Index2;
  }();
  var NotFound = /* @__PURE__ */ function() {
    function NotFound2() {
    }
    ;
    NotFound2.value = new NotFound2();
    return NotFound2;
  }();
  var toPath = function(route) {
    if (route instanceof Editor) {
      return "/editor";
    }
    ;
    if (route instanceof Index) {
      return "/";
    }
    ;
    if (route instanceof NotFound) {
      return "/not-found";
    }
    ;
    throw new Error("Failed pattern match at Generated.Route (line 31, column 3 - line 34, column 29): " + [route.constructor.name]);
  };
  var takeBefore = function(pattern) {
    return function(value) {
      var v = indexOf(pattern)(value);
      if (v instanceof Just) {
        return take(v.value0)(value);
      }
      ;
      if (v instanceof Nothing) {
        return value;
      }
      ;
      throw new Error("Failed pattern match at Generated.Route (line 71, column 3 - line 76, column 12): " + [v.constructor.name]);
    };
  };
  var takeAfter = function(pattern) {
    return function(value) {
      var v = indexOf(pattern)(value);
      if (v instanceof Just) {
        return new Just(drop(v.value0 + 1 | 0)(value));
      }
      ;
      if (v instanceof Nothing) {
        return Nothing.value;
      }
      ;
      throw new Error("Failed pattern match at Generated.Route (line 80, column 3 - line 85, column 14): " + [v.constructor.name]);
    };
  };
  var stripQuery = /* @__PURE__ */ takeBefore("?");
  var stripFragment = /* @__PURE__ */ takeBefore("#");
  var joinWithEquals = function(parts) {
    var v = uncons(parts);
    if (v instanceof Nothing) {
      return "";
    }
    ;
    if (v instanceof Just) {
      return foldl2(function(acc) {
        return function(next) {
          return acc + ("=" + next);
        };
      })(v.value0.head)(v.value0.tail);
    }
    ;
    throw new Error("Failed pattern match at Generated.Route (line 126, column 3 - line 131, column 57): " + [v.constructor.name]);
  };
  var decodeRoutePart = decodeURIComponent;
  var parseFragment = function(href2) {
    var v = takeAfter("#")(href2);
    if (v instanceof Just) {
      return new Some(decodeRoutePart(v.value0));
    }
    ;
    if (v instanceof Nothing) {
      return None.value;
    }
    ;
    throw new Error("Failed pattern match at Generated.Route (line 89, column 3 - line 94, column 18): " + [v.constructor.name]);
  };
  var parseQueryParam = function(chunk) {
    var v = uncons(split("=")(chunk));
    if (v instanceof Just) {
      return {
        key: decodeRoutePart(v.value0.head),
        value: decodeRoutePart(joinWithEquals(v.value0.tail))
      };
    }
    ;
    if (v instanceof Nothing) {
      return {
        key: "",
        value: ""
      };
    }
    ;
    throw new Error("Failed pattern match at Generated.Route (line 113, column 3 - line 122, column 8): " + [v.constructor.name]);
  };
  var parseQueryPairs = function(raw) {
    return map2(parseQueryParam)(filter(function(v) {
      return v !== "";
    })(split("&")(raw)));
  };
  var parseQuery = function(href2) {
    var v = takeAfter("?")(stripFragment(href2));
    if (v instanceof Just) {
      return parseQueryPairs(v.value0);
    }
    ;
    if (v instanceof Nothing) {
      return [];
    }
    ;
    throw new Error("Failed pattern match at Generated.Route (line 98, column 3 - line 103, column 9): " + [v.constructor.name]);
  };
  var splitSegments = function(href2) {
    return map2(decodeRoutePart)(filter(function(v) {
      return v !== "";
    })(split("/")(stripFragment(stripQuery(href2)))));
  };
  var parsePath = function(href2) {
    var v = splitSegments(href2);
    if (v.length === 1 && v[0] === "editor") {
      return Editor.value;
    }
    ;
    if (v.length === 0) {
      return Index.value;
    }
    ;
    return NotFound.value;
  };
  var parseRequest = function(href2) {
    var route = parsePath(href2);
    var path = splitSegments(href2);
    return {
      route,
      params: route,
      path,
      query: parseQuery(href2),
      fragment: parseFragment(href2),
      href: href2
    };
  };

  // output/PsSpa.Html/index.js
  var map3 = /* @__PURE__ */ map(functorArray);
  var Attribute = /* @__PURE__ */ function() {
    function Attribute2(value0, value1) {
      this.value0 = value0;
      this.value1 = value1;
    }
    ;
    Attribute2.create = function(value0) {
      return function(value1) {
        return new Attribute2(value0, value1);
      };
    };
    return Attribute2;
  }();
  var OnClick = /* @__PURE__ */ function() {
    function OnClick2(value0) {
      this.value0 = value0;
    }
    ;
    OnClick2.create = function(value0) {
      return new OnClick2(value0);
    };
    return OnClick2;
  }();
  var OnEvent = /* @__PURE__ */ function() {
    function OnEvent2(value0, value1) {
      this.value0 = value0;
      this.value1 = value1;
    }
    ;
    OnEvent2.create = function(value0) {
      return function(value1) {
        return new OnEvent2(value0, value1);
      };
    };
    return OnEvent2;
  }();
  var OnEventOptions = /* @__PURE__ */ function() {
    function OnEventOptions2(value0, value1, value2) {
      this.value0 = value0;
      this.value1 = value1;
      this.value2 = value2;
    }
    ;
    OnEventOptions2.create = function(value0) {
      return function(value1) {
        return function(value2) {
          return new OnEventOptions2(value0, value1, value2);
        };
      };
    };
    return OnEventOptions2;
  }();
  var Text = /* @__PURE__ */ function() {
    function Text2(value0) {
      this.value0 = value0;
    }
    ;
    Text2.create = function(value0) {
      return new Text2(value0);
    };
    return Text2;
  }();
  var Element = /* @__PURE__ */ function() {
    function Element2(value0, value1, value2) {
      this.value0 = value0;
      this.value1 = value1;
      this.value2 = value2;
    }
    ;
    Element2.create = function(value0) {
      return function(value1) {
        return function(value2) {
          return new Element2(value0, value1, value2);
        };
      };
    };
    return Element2;
  }();
  var Keyed = /* @__PURE__ */ function() {
    function Keyed2(value0) {
      this.value0 = value0;
    }
    ;
    Keyed2.create = function(value0) {
      return new Keyed2(value0);
    };
    return Keyed2;
  }();
  var functorAttribute = {
    map: function(lift) {
      return function(attribute) {
        if (attribute instanceof Attribute) {
          return new Attribute(attribute.value0, attribute.value1);
        }
        ;
        if (attribute instanceof OnClick) {
          return new OnClick(lift(attribute.value0));
        }
        ;
        if (attribute instanceof OnEvent) {
          return new OnEvent(attribute.value0, function(event) {
            return lift(attribute.value1(event));
          });
        }
        ;
        if (attribute instanceof OnEventOptions) {
          return new OnEventOptions(attribute.value0, attribute.value1, function(event) {
            return lift(attribute.value2(event));
          });
        }
        ;
        throw new Error("Failed pattern match at PsSpa.Html (line 87, column 24 - line 98, column 67): " + [attribute.constructor.name]);
      };
    }
  };
  var map1 = /* @__PURE__ */ map(functorAttribute);
  var functorHtml = {
    map: function(lift) {
      return function(html) {
        if (html instanceof Text) {
          return new Text(html.value0);
        }
        ;
        if (html instanceof Element) {
          return new Element(html.value0, map3(function(attribute) {
            return map1(lift)(attribute);
          })(html.value1), map3(function(child) {
            return map(functorHtml)(lift)(child);
          })(html.value2));
        }
        ;
        if (html instanceof Keyed) {
          return new Keyed({
            tag: html.value0.tag,
            attrs: map3(function(attribute) {
              return map1(lift)(attribute);
            })(html.value0.attrs),
            children: map3(function(v) {
              return new Tuple(v.value0, map(functorHtml)(lift)(v.value1));
            })(html.value0.children)
          });
        }
        ;
        throw new Error("Failed pattern match at PsSpa.Html (line 124, column 5 - line 142, column 12): " + [html.constructor.name]);
      };
    }
  };

  // output/PsSpa.Html.DSL/foreign.js
  var unsafeGetField = function(name) {
    return function(record) {
      return record[name];
    };
  };

  // output/PsSpa.Html.DSL/index.js
  var append1 = /* @__PURE__ */ append(semigroupArray);
  var toAttribute = function(dict) {
    return dict.toAttribute;
  };
  var toAttrClassName = {
    toAttribute: function(v) {
      return function(value) {
        return [new Attribute("class", value)];
      };
    }
  };
  var text = /* @__PURE__ */ function() {
    return Text.create;
  }();
  var fromAttrsNil = {
    fromAttrs: function(v) {
      return function(v1) {
        return [];
      };
    }
  };
  var fromAttrs = function(dict) {
    return dict.fromAttrs;
  };
  var fromAttrsCons = function(dictIsSymbol) {
    var reflectSymbol2 = reflectSymbol(dictIsSymbol);
    return function(dictToAttribute) {
      var toAttribute1 = toAttribute(dictToAttribute);
      return function(dictFromAttrs) {
        var fromAttrs1 = fromAttrs(dictFromAttrs);
        return {
          fromAttrs: function(v) {
            return function(rec) {
              var remainder = fromAttrs1($$Proxy.value)(rec);
              var key = reflectSymbol2($$Proxy.value);
              var typedValue = unsafeGetField(key)(rec);
              return append1(toAttribute1($$Proxy.value)(typedValue))(remainder);
            };
          }
        };
      };
    };
  };
  var element = function() {
    return function(dictFromAttrs) {
      var fromAttrs1 = fromAttrs(dictFromAttrs);
      return function(tag) {
        return function(attrs) {
          return function(children) {
            return new Element(tag, fromAttrs1($$Proxy.value)(attrs), children);
          };
        };
      };
    };
  };
  var element1 = /* @__PURE__ */ element();
  var h1 = function() {
    return function(dictFromAttrs) {
      return element1(dictFromAttrs)("h1");
    };
  };
  var main = function() {
    return function(dictFromAttrs) {
      return element1(dictFromAttrs)("main");
    };
  };
  var p = function() {
    return function(dictFromAttrs) {
      return element1(dictFromAttrs)("p");
    };
  };
  var span = function() {
    return function(dictFromAttrs) {
      return element1(dictFromAttrs)("span");
    };
  };
  var div2 = function() {
    return function(dictFromAttrs) {
      return element1(dictFromAttrs)("div");
    };
  };

  // output/Generated.Link/index.js
  var append2 = /* @__PURE__ */ append(semigroupArray);
  var link = function() {
    return function(dictFromAttrs) {
      var fromAttrs2 = fromAttrs(dictFromAttrs);
      return function(route) {
        return function(attrs) {
          return function(children) {
            return new Element("a", append2([new Attribute("href", toPath(route))])(fromAttrs2($$Proxy.value)(attrs)), children);
          };
        };
      };
    };
  };

  // output/PsSpa.Effect/index.js
  var None2 = /* @__PURE__ */ function() {
    function None3() {
    }
    ;
    None3.value = new None3();
    return None3;
  }();
  var Batch = /* @__PURE__ */ function() {
    function Batch2(value0) {
      this.value0 = value0;
    }
    ;
    Batch2.create = function(value0) {
      return new Batch2(value0);
    };
    return Batch2;
  }();
  var FromCommand = /* @__PURE__ */ function() {
    function FromCommand2(value0) {
      this.value0 = value0;
    }
    ;
    FromCommand2.create = function(value0) {
      return new FromCommand2(value0);
    };
    return FromCommand2;
  }();
  var FromShared = /* @__PURE__ */ function() {
    function FromShared2(value0) {
      this.value0 = value0;
    }
    ;
    FromShared2.create = function(value0) {
      return new FromShared2(value0);
    };
    return FromShared2;
  }();
  var Push = /* @__PURE__ */ function() {
    function Push2(value0) {
      this.value0 = value0;
    }
    ;
    Push2.create = function(value0) {
      return new Push2(value0);
    };
    return Push2;
  }();
  var Replace = /* @__PURE__ */ function() {
    function Replace2(value0) {
      this.value0 = value0;
    }
    ;
    Replace2.create = function(value0) {
      return new Replace2(value0);
    };
    return Replace2;
  }();
  var none = /* @__PURE__ */ function() {
    return None2.value;
  }();

  // output/PsSpa.Page/index.js
  var StaticPage = /* @__PURE__ */ function() {
    function StaticPage2(value0) {
      this.value0 = value0;
    }
    ;
    StaticPage2.create = function(value0) {
      return new StaticPage2(value0);
    };
    return StaticPage2;
  }();
  var SandboxPage = /* @__PURE__ */ function() {
    function SandboxPage2(value0) {
      this.value0 = value0;
    }
    ;
    SandboxPage2.create = function(value0) {
      return new SandboxPage2(value0);
    };
    return SandboxPage2;
  }();
  var ElementPage = /* @__PURE__ */ function() {
    function ElementPage2(value0) {
      this.value0 = value0;
    }
    ;
    ElementPage2.create = function(value0) {
      return new ElementPage2(value0);
    };
    return ElementPage2;
  }();
  var AdvancedPage = /* @__PURE__ */ function() {
    function AdvancedPage2(value0) {
      this.value0 = value0;
    }
    ;
    AdvancedPage2.create = function(value0) {
      return new AdvancedPage2(value0);
    };
    return AdvancedPage2;
  }();
  var $$static = /* @__PURE__ */ function() {
    return StaticPage.create;
  }();
  var advanced = /* @__PURE__ */ function() {
    return AdvancedPage.create;
  }();

  // output/Pages.Editor/index.js
  var fromAttrsCons2 = /* @__PURE__ */ fromAttrsCons({
    reflectSymbol: function() {
      return "className";
    }
  })(toAttrClassName)(fromAttrsNil);
  var main2 = /* @__PURE__ */ main()(fromAttrsCons2);
  var div3 = /* @__PURE__ */ div2()(fromAttrsCons2);
  var h12 = /* @__PURE__ */ h1()(fromAttrsCons2);
  var span2 = /* @__PURE__ */ span()(fromAttrsCons2);
  var link2 = /* @__PURE__ */ link()(fromAttrsCons2);
  var element2 = /* @__PURE__ */ element()(fromAttrsNil);
  var view = function(model) {
    return {
      title: "Verdict IDE",
      body: [main2({
        className: "h-screen w-screen flex flex-col bg-[#0b0f1a] text-slate-200 font-sans overflow-hidden"
      })([div3({
        className: "flex items-center justify-between px-5 py-2.5 bg-slate-950/90 border-b border-slate-800"
      })([div3({
        className: "flex items-center gap-3"
      })([div3({
        className: "flex h-8 w-8 items-center justify-center rounded-lg bg-gradient-to-br from-violet-400 to-indigo-600 text-sm font-black text-white shadow-lg shadow-indigo-900/40 ring-1 ring-inset ring-white/15"
      })([text("V")]), div3({
        className: "flex items-baseline gap-1.5"
      })([h12({
        className: "text-lg font-bold tracking-tight text-white"
      })([text("Verdict")]), span2({
        className: "text-lg font-light text-slate-500"
      })([text("IDE")])]), span2({
        className: "rounded-full bg-slate-800/80 px-2 py-0.5 text-[10px] font-semibold tracking-wide text-slate-400 ring-1 ring-inset ring-white/5"
      })([text("v0.1")])]), div3({
        className: "flex items-center gap-4"
      })([span2({
        className: "hidden font-mono text-xs text-slate-600 sm:block"
      })([text("Verdict \u2192 FinVM")]), link2(Index.value)({
        className: "text-sm font-medium text-slate-400 transition-colors hover:text-white"
      })([text("Home")])])]), div3({
        className: "relative flex-1 min-h-0"
      })([element2("verdict-editor")({})([])])])]
    };
  };
  var update = function(msg) {
    return function(model) {
      return {
        model: {
          status: "handled"
        },
        effect: none
      };
    };
  };
  var subscriptions = function(v) {
    return [];
  };
  var protect = function(v) {
    return function(v1) {
      return Nothing.value;
    };
  };
  var init = {
    model: {
      status: "ready"
    },
    effect: none
  };
  var page = function(v) {
    return advanced({
      init,
      update,
      view,
      subscriptions
    });
  };

  // output/Pages.Index/index.js
  var fromAttrsCons3 = /* @__PURE__ */ fromAttrsCons({
    reflectSymbol: function() {
      return "className";
    }
  })(toAttrClassName)(fromAttrsNil);
  var span3 = /* @__PURE__ */ span()(fromAttrsCons3);
  var div4 = /* @__PURE__ */ div2()(fromAttrsCons3);
  var view2 = /* @__PURE__ */ function() {
    var chip = function(label) {
      return span3({
        className: "rounded-full border border-slate-700/70 bg-slate-800/40 px-3 py-1 text-xs font-medium text-slate-300"
      })([text(label)]);
    };
    return {
      title: "Home",
      body: [main()(fromAttrsCons3)({
        className: "relative flex min-h-screen flex-col items-center justify-center gap-7 overflow-hidden bg-[#0b0f1a] px-6 py-16 text-center font-sans"
      })([div4({
        className: "pointer-events-none absolute -top-40 left-1/2 h-96 w-[40rem] -translate-x-1/2 rounded-full bg-indigo-600/20 blur-[120px]"
      })([]), div4({
        className: "relative flex h-16 w-16 items-center justify-center rounded-2xl bg-gradient-to-br from-violet-400 to-indigo-600 text-3xl font-black text-white shadow-2xl shadow-indigo-900/50 ring-1 ring-inset ring-white/20"
      })([text("V")]), div4({
        className: "relative flex flex-col gap-3"
      })([h1()(fromAttrsCons3)({
        className: "text-5xl font-extrabold tracking-tight text-white"
      })([text("Verdict"), span3({
        className: "text-slate-600"
      })([text(" IDE")])]), p()(fromAttrsCons3)({
        className: "mx-auto max-w-xl text-lg text-slate-400"
      })([text("An intelligent editor for the Verdict language \u2014 with live types, instant diagnostics, and inline results as you type.")])]), div4({
        className: "relative flex flex-wrap items-center justify-center gap-2"
      })([chip("Type-on-hover"), chip("Live diagnostics"), chip("Inline results"), chip("Runs on FinVM")]), link()(fromAttrsCons3)(Editor.value)({
        className: "relative mt-2 inline-flex w-fit items-center gap-2 rounded-full bg-gradient-to-b from-violet-500 to-indigo-600 px-7 py-3 text-sm font-bold uppercase tracking-wider text-white shadow-xl shadow-indigo-900/40 ring-1 ring-inset ring-white/15 transition-all hover:from-violet-400 hover:to-indigo-500 hover:shadow-indigo-700/50"
      })([text("Open Editor \u2192")])])]
    };
  }();
  var protect2 = function(v) {
    return function(v1) {
      return Nothing.value;
    };
  };
  var page2 = function(v) {
    return $$static({
      view: view2
    });
  };

  // output/Pages.NotFound/index.js
  var fromAttrsCons4 = /* @__PURE__ */ fromAttrsCons({
    reflectSymbol: function() {
      return "className";
    }
  })(toAttrClassName)(fromAttrsNil);
  var view3 = /* @__PURE__ */ function() {
    return {
      title: "Not Found",
      body: [main()(fromAttrsCons4)({
        className: "mx-auto flex min-h-screen max-w-4xl flex-col justify-center gap-6 px-6 py-16"
      })([h1()(fromAttrsCons4)({
        className: "text-4xl font-bold tracking-tight text-slate-950"
      })([text("Not Found")]), p()(fromAttrsCons4)({
        className: "text-lg text-slate-600"
      })([text("Generated static page for route /not-found.")]), link()(fromAttrsCons4)(Index.value)({
        className: "w-fit rounded-full border border-slate-300 px-5 py-3 text-sm font-semibold text-slate-800"
      })([text("Back home")])])]
    };
  }();
  var protect3 = function(v) {
    return function(v1) {
      return Nothing.value;
    };
  };
  var page3 = function(v) {
    return $$static({
      view: view3
    });
  };

  // output/PsSpa.LoadResult/index.js
  var Redirect = /* @__PURE__ */ function() {
    function Redirect2(value0) {
      this.value0 = value0;
    }
    ;
    Redirect2.create = function(value0) {
      return new Redirect2(value0);
    };
    return Redirect2;
  }();
  var Loaded = /* @__PURE__ */ function() {
    function Loaded2(value0) {
      this.value0 = value0;
    }
    ;
    Loaded2.create = function(value0) {
      return new Loaded2(value0);
    };
    return Loaded2;
  }();

  // output/PsSpa.LoadedPage/index.js
  var withPage = function(run3) {
    return function(v) {
      return v(run3);
    };
  };
  var fromPage = function(page4) {
    return function(run3) {
      return run3(page4);
    };
  };

  // output/Generated.Pages/index.js
  var decide = function(load) {
    return function(protect4) {
      return function(shared) {
        return function(request) {
          var v = protect4(shared)(request);
          if (v instanceof Just) {
            return new Redirect(v.value0);
          }
          ;
          if (v instanceof Nothing) {
            return new Loaded(fromPage(load(request)));
          }
          ;
          throw new Error("Failed pattern match at Generated.Pages (line 60, column 3 - line 65, column 61): " + [v.constructor.name]);
        };
      };
    };
  };
  var loadPage = function(shared) {
    return function(request) {
      if (request.route instanceof Editor) {
        return decide(page)(protect)(shared)(request);
      }
      ;
      if (request.route instanceof Index) {
        return decide(page2)(protect2)(shared)(request);
      }
      ;
      if (request.route instanceof NotFound) {
        return decide(page3)(protect3)(shared)(request);
      }
      ;
      throw new Error("Failed pattern match at Generated.Pages (line 47, column 3 - line 50, column 77): " + [request.route.constructor.name]);
    };
  };

  // output/PsSpa.Browser/foreign.js
  function isObject(value) {
    return value !== null && typeof value === "object";
  }
  function isTextHtml(node) {
    return isObject(node) && "value0" in node && !("value1" in node) && typeof node.value0 === "string";
  }
  function isKeyedHtml(node) {
    return isObject(node) && "value0" in node && !("value1" in node) && isObject(node.value0);
  }
  function isElementHtml(node) {
    return isObject(node) && "value2" in node;
  }
  var SVG_NS = "http://www.w3.org/2000/svg";
  var MATH_NS = "http://www.w3.org/1998/Math/MathML";
  var HTML_NS = "http://www.w3.org/1999/xhtml";
  function namespaceForTag(tag, parentNamespace) {
    var lower = String(tag).toLowerCase();
    if (lower === "svg") return SVG_NS;
    if (lower === "math") return MATH_NS;
    return parentNamespace || null;
  }
  function childNamespaceForTag(tag, namespace) {
    var lower = String(tag).toLowerCase();
    if (namespace === SVG_NS && lower === "foreignobject") return null;
    return namespace;
  }
  function createElement(tag, namespace) {
    if (namespace) {
      return document.createElementNS(namespace, tag);
    }
    return document.createElement(tag);
  }
  function createNode(html, parentNamespace) {
    if (!isObject(html)) return document.createTextNode("");
    if (isTextHtml(html)) return document.createTextNode(html.value0);
    if (isElementHtml(html)) {
      var namespace = namespaceForTag(html.value0, parentNamespace);
      var element3 = createElement(html.value0, namespace);
      var childNamespace = childNamespaceForTag(html.value0, namespace);
      applyAttributes(element3, [], html.value1);
      for (var i = 0; i < html.value2.length; i += 1) {
        element3.appendChild(createNode(html.value2[i], childNamespace));
      }
      return element3;
    }
    if (isKeyedHtml(html)) {
      var record = html.value0;
      var keyedNamespace = namespaceForTag(record.tag, parentNamespace);
      var keyedChildNamespace = childNamespaceForTag(record.tag, keyedNamespace);
      var keyedEl = createElement(record.tag, keyedNamespace);
      applyAttributes(keyedEl, [], record.attrs);
      var keyMap = {};
      for (var j = 0; j < record.children.length; j += 1) {
        var pair = record.children[j];
        var pairKey = pair.value0;
        if (keyMap[pairKey] !== void 0) {
          keyedEl.removeChild(keyMap[pairKey]);
        }
        var childNode = createNode(pair.value1, keyedChildNamespace);
        keyedEl.appendChild(childNode);
        keyMap[pairKey] = childNode;
      }
      keyedEl._psSpaKeyMap = keyMap;
      return keyedEl;
    }
    return document.createTextNode("");
  }
  function reconcileChildren(parent, nextChildren, namespace) {
    var existing = [];
    for (var k = 0; k < parent.childNodes.length; k += 1) {
      existing.push(parent.childNodes[k]);
    }
    var max2 = existing.length > nextChildren.length ? existing.length : nextChildren.length;
    for (var i = 0; i < max2; i += 1) {
      var domNode = existing[i];
      var nextHtml = nextChildren[i];
      if (nextHtml === void 0) {
        parent.removeChild(domNode);
      } else if (domNode === void 0) {
        parent.appendChild(createNode(nextHtml, namespace));
      } else {
        patchNode(parent, domNode, nextHtml, namespace);
      }
    }
  }
  function reconcileKeyedChildren(parent, nextPairs, namespace) {
    var oldKeyMap = parent._psSpaKeyMap || {};
    var nextKeyMap = {};
    var nextKeySet = /* @__PURE__ */ Object.create(null);
    for (var i = 0; i < nextPairs.length; i += 1) {
      nextKeySet[nextPairs[i].value0] = true;
    }
    for (var oldKey in oldKeyMap) {
      if (!nextKeySet[oldKey]) {
        var stale = oldKeyMap[oldKey];
        if (stale.parentNode === parent) {
          parent.removeChild(stale);
        }
      }
    }
    for (var j = 0; j < nextPairs.length; j += 1) {
      var pair = nextPairs[j];
      var key = pair.value0;
      var nextHtml = pair.value1;
      var existing = oldKeyMap[key];
      var resolved;
      if (existing && existing.parentNode === parent) {
        resolved = patchNode(parent, existing, nextHtml, namespace) || existing;
      } else {
        resolved = createNode(nextHtml, namespace);
      }
      nextKeyMap[key] = resolved;
      var currentAtJ = parent.childNodes[j];
      if (currentAtJ !== resolved) {
        parent.insertBefore(resolved, currentAtJ || null);
      }
    }
    while (parent.childNodes.length > nextPairs.length) {
      parent.removeChild(parent.childNodes[parent.childNodes.length - 1]);
    }
    parent._psSpaKeyMap = nextKeyMap;
  }
  function sameElementTag(domNode, tag, namespace) {
    if (domNode.nodeType !== 1) return false;
    if (normalizeNamespace(domNode.namespaceURI) !== normalizeNamespace(namespace)) return false;
    return domNode.nodeName.toLowerCase() === String(tag).toLowerCase();
  }
  function normalizeNamespace(namespace) {
    return namespace === HTML_NS ? null : namespace || null;
  }
  function patchNode(parent, domNode, nextHtml, parentNamespace) {
    if (isTextHtml(nextHtml)) {
      if (domNode.nodeType === 3) {
        if (domNode.nodeValue !== nextHtml.value0) {
          domNode.nodeValue = nextHtml.value0;
        }
        return domNode;
      }
      var freshText = document.createTextNode(nextHtml.value0);
      parent.replaceChild(freshText, domNode);
      return freshText;
    }
    if (isKeyedHtml(nextHtml)) {
      var record = nextHtml.value0;
      var keyedNamespace = namespaceForTag(record.tag, parentNamespace);
      var keyedChildNamespace = childNamespaceForTag(record.tag, keyedNamespace);
      if (!sameElementTag(domNode, record.tag, keyedNamespace)) {
        var freshKeyed = createNode(nextHtml, parentNamespace);
        parent.replaceChild(freshKeyed, domNode);
        return freshKeyed;
      }
      var keyedScrollTop = domNode.scrollTop;
      var keyedScrollLeft = domNode.scrollLeft;
      var previousAttrsKeyed = domNode._psSpaAttrs || [];
      applyAttributes(domNode, previousAttrsKeyed, record.attrs);
      reconcileKeyedChildren(domNode, record.children, keyedChildNamespace);
      restoreScrollPosition(domNode, keyedScrollTop, keyedScrollLeft);
      return domNode;
    }
    if (!isElementHtml(nextHtml)) {
      var emptyText = document.createTextNode("");
      parent.replaceChild(emptyText, domNode);
      return emptyText;
    }
    var nextTag = nextHtml.value0;
    var namespace = namespaceForTag(nextTag, parentNamespace);
    var childNamespace = childNamespaceForTag(nextTag, namespace);
    if (!sameElementTag(domNode, nextTag, namespace)) {
      var freshEl = createNode(nextHtml, parentNamespace);
      parent.replaceChild(freshEl, domNode);
      return freshEl;
    }
    var previousScrollTop = domNode.scrollTop;
    var previousScrollLeft = domNode.scrollLeft;
    var previousAttrs = domNode._psSpaAttrs || [];
    applyAttributes(domNode, previousAttrs, nextHtml.value1);
    if (domNode._psSpaKeyMap) {
      domNode._psSpaKeyMap = null;
    }
    reconcileChildren(domNode, nextHtml.value2, childNamespace);
    restoreScrollPosition(domNode, previousScrollTop, previousScrollLeft);
    return domNode;
  }
  function restoreScrollPosition(element3, scrollTop, scrollLeft) {
    if (typeof scrollTop === "number" && element3.scrollTop !== scrollTop) {
      element3.scrollTop = scrollTop;
    }
    if (typeof scrollLeft === "number" && element3.scrollLeft !== scrollLeft) {
      element3.scrollLeft = scrollLeft;
    }
  }
  function setDomAttribute(element3, name, value) {
    if (element3.getAttribute(name) !== value) {
      element3.setAttribute(name, value);
    }
    if (name === "value" || name === "checked" || name === "selected" || name === "muted") {
      syncDomProperty(element3, name, value);
    }
  }
  function removeDomAttribute(element3, name) {
    element3.removeAttribute(name);
    if (name === "value") {
      element3.value = "";
    } else if (name === "checked" || name === "selected" || name === "muted") {
      element3[name] = false;
    }
  }
  function syncDomProperty(element3, name, value) {
    if (name === "value") {
      if (element3.value !== value) element3.value = value;
    } else if (name === "checked" || name === "selected" || name === "muted") {
      element3[name] = value !== "false" && value !== "";
    }
  }
  function applyAttributes(element3, previousAttrs, nextAttrs) {
    var previousListeners = element3._psSpaListeners || {};
    var nextListeners = {};
    var nextListenerCounts = {};
    var previousAttrNames = {};
    var nextAttrNames = {};
    for (var p2 = 0; p2 < previousAttrs.length; p2 += 1) {
      var prev = previousAttrs[p2];
      if (isObject(prev) && !("value2" in prev) && "value1" in prev && typeof prev.value1 !== "function") {
        previousAttrNames[prev.value0] = true;
      }
    }
    for (var i = 0; i < nextAttrs.length; i += 1) {
      var attr = nextAttrs[i];
      if (!isObject(attr)) continue;
      if ("value2" in attr && typeof attr.value2 === "function") {
        var optionEventName = attr.value0;
        var options = attr.value1 || {};
        var optionHandler = attr.value2;
        var optionListenerKey = nextListenerKey(nextListenerCounts, optionEventName);
        nextListeners[optionListenerKey] = upsertEventListener(
          element3,
          previousListeners[optionListenerKey],
          optionEventName,
          "options",
          options,
          /* @__PURE__ */ function(opts, h) {
            return function(event) {
              if (opts.preventDefault && event && typeof event.preventDefault === "function") {
                event.preventDefault();
              }
              if (opts.stopPropagation && event && typeof event.stopPropagation === "function") {
                event.stopPropagation();
              }
              h(event)();
            };
          }(options, optionHandler)
        );
      } else if ("value1" in attr && typeof attr.value1 === "function") {
        var eventName = attr.value0;
        var handler = attr.value1;
        var listenerKey = nextListenerKey(nextListenerCounts, eventName);
        nextListeners[listenerKey] = upsertEventListener(
          element3,
          previousListeners[listenerKey],
          eventName,
          "event",
          null,
          /* @__PURE__ */ function(h) {
            return function(event) {
              h(event)();
            };
          }(handler)
        );
      } else if ("value1" in attr) {
        var attrName = attr.value0;
        nextAttrNames[attrName] = true;
        setDomAttribute(element3, attrName, attr.value1);
      } else if ("value0" in attr) {
        var message = attr.value0;
        var clickListenerKey = nextListenerKey(nextListenerCounts, "click");
        nextListeners[clickListenerKey] = upsertEventListener(
          element3,
          previousListeners[clickListenerKey],
          "click",
          "click",
          null,
          /* @__PURE__ */ function(m) {
            return function(_event) {
              m();
            };
          }(message)
        );
      }
    }
    for (var name in previousAttrNames) {
      if (!nextAttrNames[name]) {
        removeDomAttribute(element3, name);
      }
    }
    for (var listenerName in previousListeners) {
      if (!nextListeners[listenerName]) {
        removeStoredListener(element3, listenerName, previousListeners[listenerName]);
      }
    }
    element3._psSpaAttrs = nextAttrs;
    element3._psSpaListeners = nextListeners;
  }
  function nextListenerKey(counts, eventName) {
    var index = counts[eventName] || 0;
    counts[eventName] = index + 1;
    return eventName + "#" + index;
  }
  function upsertEventListener(element3, previous, eventName, kind4, options, invoke) {
    if (previous && previous.eventName === eventName && previous.kind === kind4 && sameEventOptions(previous.options, options)) {
      previous.invoke = invoke;
      return previous;
    }
    if (previous) {
      removeStoredListener(element3, eventName, previous);
    }
    var record = {
      eventName,
      kind: kind4,
      options: cloneEventOptions(options),
      invoke,
      listener: null
    };
    record.listener = function(event) {
      record.invoke(event);
    };
    element3.addEventListener(eventName, record.listener);
    return record;
  }
  function removeStoredListener(element3, listenerName, stored) {
    if (stored && stored.listener) {
      element3.removeEventListener(stored.eventName, stored.listener);
    } else if (typeof stored === "function") {
      element3.removeEventListener(listenerName, stored);
    }
  }
  function cloneEventOptions(options) {
    if (!options) return null;
    return {
      preventDefault: Boolean(options.preventDefault),
      stopPropagation: Boolean(options.stopPropagation)
    };
  }
  function sameEventOptions(left, right) {
    var a2 = cloneEventOptions(left);
    var b = cloneEventOptions(right);
    if (!a2 && !b) return true;
    if (!a2 || !b) return false;
    return a2.preventDefault === b.preventDefault && a2.stopPropagation === b.stopPropagation;
  }
  var renderDocument = function(config) {
    return function() {
      var root = document.getElementById(config.rootId) || document.body;
      if (!root) {
        return;
      }
      document.title = config.document.title;
      reconcileChildren(root, config.document.body, null);
    };
  };
  var currentPath = function() {
    return window.location.pathname + window.location.search + window.location.hash;
  };
  var pushUrl = function(url) {
    return function() {
      window.history.pushState({}, "", url);
    };
  };
  var replaceUrl = function(url) {
    return function() {
      window.history.replaceState({}, "", url);
    };
  };
  var onPopState = function(handler) {
    return function() {
      var listener = function() {
        handler();
      };
      window.addEventListener("popstate", listener);
      return function() {
        window.removeEventListener("popstate", listener);
      };
    };
  };
  var onInternalUrlRequest = function(handler) {
    return function() {
      var listener = function(event) {
        if (event.defaultPrevented) {
          return;
        }
        if (event.button !== 0 || event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) {
          return;
        }
        var node = event.target;
        while (node && node !== document.body) {
          if (node.tagName === "A") {
            var href2 = node.getAttribute("href");
            if (!href2 || href2.charAt(0) === "#") {
              return;
            }
            if (node.getAttribute("target") || node.getAttribute("download") !== null) {
              return;
            }
            var url;
            try {
              url = new URL(href2, window.location.href);
            } catch (_error) {
              return;
            }
            if (url.origin !== window.location.origin || url.protocol !== "http:" && url.protocol !== "https:") {
              return;
            }
            event.preventDefault();
            handler(url.pathname + url.search + url.hash)();
            return;
          }
          node = node.parentNode;
        }
      };
      document.addEventListener("click", listener);
      return function() {
        document.removeEventListener("click", listener);
      };
    };
  };

  // output/PsSpa.View/index.js
  var map12 = /* @__PURE__ */ map(functorArray);
  var map22 = /* @__PURE__ */ map(functorHtml);
  var map4 = function(lift) {
    return function(document2) {
      return {
        title: document2.title,
        body: map12(function(node) {
          return map22(lift)(node);
        })(document2.body)
      };
    };
  };

  // output/PsSpa.Runtime/index.js
  var $runtime_lazy2 = function(name, moduleName, init3) {
    var state = 0;
    var val;
    return function(lineNumber) {
      if (state === 2) return val;
      if (state === 1) throw new ReferenceError(name + " was needed before it finished initializing (module " + moduleName + ", line " + lineNumber + ")", moduleName, lineNumber);
      state = 1;
      val = init3();
      state = 2;
      return val;
    };
  };
  var pure3 = /* @__PURE__ */ pure(applicativeEffect);
  var traverse_2 = /* @__PURE__ */ traverse_(applicativeEffect)(foldableArray);
  var traverse2 = /* @__PURE__ */ traverse(traversableArray)(applicativeEffect);
  var identity4 = /* @__PURE__ */ identity(categoryFn);
  var all2 = /* @__PURE__ */ all(foldableArray)(heytingAlgebraBoolean);
  var applySecond2 = /* @__PURE__ */ applySecond(applyEffect);
  var when2 = /* @__PURE__ */ when(applicativeEffect);
  var start = function(config) {
    return function __do() {
      var sharedRef = $$new(config.initialShared)();
      var pageCleanupRef = $$new(pure3(unit))();
      var sharedCleanupRef = $$new(pure3(unit))();
      var runCommands = function(commands) {
        return traverse_2(config.onCommand)(commands);
      };
      var installSubscriptions = function(handle) {
        return function(subscriptions2) {
          return function __do2() {
            var cleanups = traverse2(config.onSubscription(handle))(subscriptions2)();
            return traverse_2(identity4)(cleanups);
          };
        };
      };
      var installSharedSubscriptions = function(subscriptions2) {
        return installSubscriptions(absurd)(subscriptions2);
      };
      var cleanupShared = function __do2() {
        var cleanup = read(sharedCleanupRef)();
        return cleanup();
      };
      var cleanupPage = function __do2() {
        var cleanup = read(pageCleanupRef)();
        return cleanup();
      };
      var renderLoaded = function(request) {
        return function(loaded) {
          return withPage(function(page4) {
            if (page4 instanceof StaticPage) {
              return function __do2() {
                renderDocument({
                  rootId: config.rootId,
                  document: map4($$const(pure3(unit)))(page4.value0.view)
                })();
                return pure3(unit);
              };
            }
            ;
            if (page4 instanceof SandboxPage) {
              return function __do2() {
                var modelRef = $$new(page4.value0.init)();
                var handle = function(msg) {
                  return function __do3() {
                    var model = read(modelRef)();
                    write(page4.value0.update(msg)(model))(modelRef)();
                    return $lazy_render(99)();
                  };
                };
                var $lazy_render = $runtime_lazy2("render", "PsSpa.Runtime", function() {
                  return function __do3() {
                    var model = read(modelRef)();
                    return renderDocument({
                      rootId: config.rootId,
                      document: map4(handle)(page4.value0.view(model))
                    })();
                  };
                });
                var render = $lazy_render(89);
                render();
                return pure3(unit);
              };
            }
            ;
            if (page4 instanceof ElementPage) {
              return function __do2() {
                var modelRef = $$new(page4.value0.init.model)();
                var subCleanupRef = $$new(pure3(unit))();
                var handle = function(msg) {
                  return function __do3() {
                    var model = read(modelRef)();
                    var step = page4.value0.update(msg)(model);
                    write(step.model)(modelRef)();
                    runCommands(step.effect)();
                    return $lazy_render(125)();
                  };
                };
                var $lazy_render = $runtime_lazy2("render", "PsSpa.Runtime", function() {
                  return function __do3() {
                    var previous = read(subCleanupRef)();
                    previous();
                    var model = read(modelRef)();
                    var cleanup = installSubscriptions(handle)(page4.value0.subscriptions(model))();
                    write(cleanup)(subCleanupRef)();
                    return renderDocument({
                      rootId: config.rootId,
                      document: map4(handle)(page4.value0.view(model))
                    })();
                  };
                });
                var render = $lazy_render(109);
                runCommands(page4.value0.init.effect)();
                render();
                return function __do3() {
                  var cleanup = read(subCleanupRef)();
                  return cleanup();
                };
              };
            }
            ;
            if (page4 instanceof AdvancedPage) {
              return function __do2() {
                var modelRef = $$new(page4.value0.init.model)();
                var subCleanupRef = $$new(pure3(unit))();
                var keepsCurrentPage = function(activeEffect) {
                  if (activeEffect instanceof None2) {
                    return true;
                  }
                  ;
                  if (activeEffect instanceof Batch) {
                    return all2(keepsCurrentPage)(activeEffect.value0);
                  }
                  ;
                  if (activeEffect instanceof FromCommand) {
                    return true;
                  }
                  ;
                  if (activeEffect instanceof FromShared) {
                    return false;
                  }
                  ;
                  if (activeEffect instanceof Push) {
                    return false;
                  }
                  ;
                  if (activeEffect instanceof Replace) {
                    return false;
                  }
                  ;
                  throw new Error("Failed pattern match at PsSpa.Runtime (line 179, column 19 - line 196, column 28): " + [activeEffect.constructor.name]);
                };
                var interpretEffect = function(effect) {
                  if (effect instanceof None2) {
                    return pure3(true);
                  }
                  ;
                  if (effect instanceof Batch) {
                    return function __do3() {
                      traverse_2(interpretEffect)(effect.value0)();
                      return keepsCurrentPage(effect);
                    };
                  }
                  ;
                  if (effect instanceof FromCommand) {
                    return applySecond2(config.onCommand(effect.value0))(pure3(true));
                  }
                  ;
                  if (effect instanceof FromShared) {
                    return applySecond2(applySecond2(write(effect.value0)(sharedRef))(openRequest(request)))(pure3(false));
                  }
                  ;
                  if (effect instanceof Push) {
                    return applySecond2(goWith(pushUrl)(effect.value0))(pure3(false));
                  }
                  ;
                  if (effect instanceof Replace) {
                    return applySecond2(goWith(replaceUrl)(effect.value0))(pure3(false));
                  }
                  ;
                  throw new Error("Failed pattern match at PsSpa.Runtime (line 157, column 19 - line 176, column 72): " + [effect.constructor.name]);
                };
                var handle = function(msg) {
                  return function __do3() {
                    var model = read(modelRef)();
                    var step = page4.value0.update(msg)(model);
                    write(step.model)(modelRef)();
                    var keepCurrentPage = interpretEffect(step.effect)();
                    return when2(keepCurrentPage)($lazy_render(154))();
                  };
                };
                var $lazy_render = $runtime_lazy2("render", "PsSpa.Runtime", function() {
                  return function __do3() {
                    var previous = read(subCleanupRef)();
                    previous();
                    var model = read(modelRef)();
                    var cleanup = installSubscriptions(handle)(page4.value0.subscriptions(model))();
                    write(cleanup)(subCleanupRef)();
                    return renderDocument({
                      rootId: config.rootId,
                      document: map4(handle)(page4.value0.view(model))
                    })();
                  };
                });
                var render = $lazy_render(138);
                interpretEffect(page4.value0.init.effect)();
                render();
                return function __do3() {
                  var cleanup = read(subCleanupRef)();
                  return cleanup();
                };
              };
            }
            ;
            throw new Error("Failed pattern match at PsSpa.Runtime (line 77, column 11 - line 202, column 24): " + [page4.constructor.name]);
          })(loaded);
        };
      };
      var openRequest = function(request) {
        return function __do2() {
          cleanupPage();
          cleanupShared();
          var shared = read(sharedRef)();
          var v = config.loadPage(shared)(request);
          if (v instanceof Redirect) {
            replaceUrl(config.toPath(v.value0))();
            return openRequest(config.parseRequest(config.toPath(v.value0)))();
          }
          ;
          if (v instanceof Loaded) {
            var cleanup = renderLoaded(request)(v.value0)();
            write(cleanup)(pageCleanupRef)();
            var latestShared = read(sharedRef)();
            var sharedCleanup = installSharedSubscriptions(config.sharedSubscriptions(request)(latestShared))();
            return write(sharedCleanup)(sharedCleanupRef)();
          }
          ;
          throw new Error("Failed pattern match at PsSpa.Runtime (line 210, column 7 - line 220, column 51): " + [v.constructor.name]);
        };
      };
      var goWith = function(pushOrReplace) {
        return function(route) {
          return function __do2() {
            pushOrReplace(config.toPath(route))();
            return openRequest(config.parseRequest(config.toPath(route)))();
          };
        };
      };
      var openCurrentPath = function __do2() {
        var href2 = currentPath();
        return openRequest(config.parseRequest(href2))();
      };
      onPopState(openCurrentPath)();
      onInternalUrlRequest(function(href2) {
        return function __do2() {
          pushUrl(href2)();
          return openRequest(config.parseRequest(href2))();
        };
      })();
      return openCurrentPath();
    };
  };

  // output/Generated.App/index.js
  var startWith = function(config) {
    return start({
      initialShared: config.initialShared,
      loadPage,
      onCommand: config.onCommand,
      onSubscription: config.onSubscription,
      parseRequest,
      rootId: config.rootId,
      sharedSubscriptions: config.sharedSubscriptions,
      toPath
    });
  };
  var startWithShared = function(initialShared) {
    return startWith({
      initialShared,
      onCommand,
      onSubscription,
      rootId: "app",
      sharedSubscriptions: function(v) {
        return function(v1) {
          return [];
        };
      }
    });
  };

  // output/Shared/index.js
  var init2 = /* @__PURE__ */ function() {
    return {
      currentUser: Nothing.value
    };
  }();

  // output/Main/index.js
  var main3 = /* @__PURE__ */ startWithShared(init2);

  // <stdin>
  main3();
})();
